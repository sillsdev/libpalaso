Do a technical review of all open issues. Have cheaper subagents evaluate/estimate scope of a fix, including: size; files to add/edit; review difficulty; if breaking change; if already fixed in `master`. When evaluating an issue, consider the full issue converation. Save the results to a new .md file in this `issues` branch. The top of the file should have a few sections: summary table with issues sorted by size; section on sets of issues that alter the same file; summary of the couple in-progress prs. Do not change any pre-existing branches or files.
