<project name="PalasoUIWindowsForms.TestApp" default="build" basedir=".">
  <target name="build">
	<csc target="exe"
		output="${project.output.dir}/PalasoUIWindowsForms.TestApp.exe"
		doc="${project.output.dir}/doc/PalasoUIWindowsForms.TestApp.xml"
		debug="${build.debug}"
		define="${build.defines}">
	  <nowarn>
		<warning number="618,672"/>
		<warning number="1699" if="${runtime.version=='2.0'}"/>
	  </nowarn>
	  <sources basedir=".">
		<include name="**/*.cs" />
	  </sources>
	  <references basedir="${project.output.dir}">
		<include name="System.Windows.Forms.dll" />
		<include name="PalasoUIWindowsForms.dll" />
	  </references>
	</csc>
  </target>

  <target name="package">
	<copy todir="${package.src.dir}/icu.net">
	  <fileset refid="source-files" />
	</copy>
  </target>

</project>
